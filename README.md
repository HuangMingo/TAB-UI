[Tiếng Việt]
📝 Giới thiệu
Đây là một dự án luyện tập Frontend tập trung vào việc xây dựng giao diện người dùng (UI) và xử lý logic bằng JavaScript thuần (Vanilla JS). Dự án bao gồm hai thành phần chính:

Tab UI: Hệ thống chuyển đổi tab mượt mà với hiệu ứng thanh trượt thông minh.

Music Player Interface: Giao diện phát nhạc hiện đại được lấy cảm hứng từ các ứng dụng streaming phổ biến, tối ưu hóa cho trải nghiệm người dùng.

🚀 Tính năng nổi bật
Chuyển đổi Tab động: Thay đổi nội dung hiển thị tương ứng khi nhấn vào các tiêu đề Tab (React, Angular, Ember, Vue).

Thanh trượt thông minh (Smart Line): Thanh chỉ báo dưới mỗi tab tự động tính toán vị trí (left) và độ rộng (width) dựa trên phần tử đang hoạt động.

Tương thích màn hình (Responsive Design): Xử lý sự kiện window.onresize để đảm bảo thanh trượt luôn nằm đúng vị trí khi người dùng thay đổi kích thước trình duyệt hoặc đóng/mở DevTools.

Giao diện phát nhạc hiện đại: Sử dụng CSS nâng cao (Flexbox, RGBA, Border-radius) để tạo ra các nút điều khiển và đĩa nhạc đẹp mắt.

🛠 Công nghệ sử dụng
HTML5: Cấu trúc ngữ nghĩa cho trang web.

CSS3: Sử dụng Flexbox, Absolute Positioning, và CSS Transitions cho hiệu ứng mượt mà.

JavaScript (ES6+): Xử lý DOM Manipulation, Event Listeners, và logic tính toán tọa độ phần tử.

[English]
📝 Description
This is a Frontend practice project focused on building user interfaces (UI) and handling logic using Vanilla JavaScript. The project consists of two main components:

Tab UI: A smooth tab switching system with a smart sliding underline effect.

Music Player Interface: A modern music player UI inspired by popular streaming apps, optimized for user experience.

🚀 Key Features
Dynamic Tab Switching: Displays corresponding content when clicking on Tab items (React, Angular, Ember, Vue).

Smart Indicator Line: An underline indicator that automatically calculates its left position and width based on the active element.

Responsive Handling: Listens to the window.onresize event to ensure the indicator line stays perfectly aligned even when the browser is resized or DevTools is toggled.

Modern Music UI: Utilizes advanced CSS (Flexbox, RGBA, Border-radius) to create aesthetically pleasing controls and album art displays.

🛠 Tech Stack
HTML5: Semantic web structure.

CSS3: Flexbox layout, Absolute Positioning, and CSS Transitions for smooth animations.

JavaScript (ES6+): DOM Manipulation, Event Listeners, and element coordinate calculation logic.

📸 Screenshots
<img width="590" height="248" alt="image" src="https://github.com/user-attachments/assets/b9b9373d-b97f-4397-b2fb-79861ae27779" />
<img width="674" height="243" alt="image" src="https://github.com/user-attachments/assets/5188a990-69a0-40d5-91e3-2d12c57ce4d1" />

